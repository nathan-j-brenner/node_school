{
  "name": "ts_expressworks",
  "version": "1.0.0",
  "description": "already done: installed node/npm npm install -g typescript npm install -g tsd",
  "main": "index.js",
  "scripts": {
    "tsc": "tsc",
    "tsc:w": "tsc -w --outDir dist",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run tsc:w"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.13.4"
  }
}
